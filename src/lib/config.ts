import { dev } from "$app/environment";

export const title: string = "Simplify"
export const description: string = "Sharing information about our universe"
export const url: string = dev ? "https//localhost:3000" : "url"; 